export class Menu {
    public id: string;
    public type: string;
    public name: string;
      public price: string;
      constructor(id,type, nombre, precio) {
          this.id = id;
          this.id = type;
          this.name = nombre;
          this.price = precio;
      }
  }